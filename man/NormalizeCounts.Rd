% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pseudobulk.R
\name{NormalizeCounts}
\alias{NormalizeCounts}
\title{NormalizeCounts}
\usage{
NormalizeCounts(
  se,
  method = c("CPM", "logCPM", "logNorm", "VST", "rlog"),
  assay_name = "counts",
  new_assay_name = NULL,
  pseudocount = 1,
  ...
)
}
\arguments{
\item{se}{A SummarizedExperiment containing a raw counts assay.}

\item{method}{Character; one of "CPM", "logCPM", "logNorm", "VST", "rlog".
\itemize{
\item "CPM": counts per million.
\item "logCPM": log2(CPM + pseudocount).
\item "logNorm": Seurat-style log1p(counts / size_factor * 1e4) where
size_factor = colSums(counts) / median(colSums(counts)).
\item "VST", "rlog": variance-stabilizing transform or rlog via DESeq2.
}}

\item{assay_name}{Character scalar; name of the assay in \code{se} to normalize
(default: "counts").}

\item{new_assay_name}{Character or NULL; name to assign the normalized assay.
If NULL, defaults to the chosen \code{method}.}

\item{pseudocount}{Numeric scalar added to CPM before log2 in "logCPM"
(default: 1).}

\item{...}{Additional arguments forwarded to DESeq2::vst or DESeq2::rlog when
\code{method} is "VST" or "rlog".}
}
\value{
A SummarizedExperiment identical to \code{se} but with a new assay named
\code{new_assay_name} containing the normalized matrix.
}
\description{
Perform per-sample normalization of a count assay stored in a
SummarizedExperiment and add the normalized matrix as a new assay.
}
\details{
The function checks that \code{se} is a SummarizedExperiment and that
\code{assay_name} exists and is a (possibly sparse) matrix. For "VST" and
"rlog", DESeq2 must be installed; the function converts the assay to a
dense matrix and constructs a DESeqDataSet with design ~ 1 before
applying the transform. Errors are raised for invalid inputs.
}
\examples{
# Basic usage (assuming `se` is a SummarizedExperiment with a "counts" assay)
# se_norm <- NormalizeSE(se, method = "logCPM")

}
\seealso{
DESeq2::vst, DESeq2::rlog
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pseudobulk.R
\name{AggregatePseudobulk}
\alias{AggregatePseudobulk}
\title{AggregatePseudobulk}
\usage{
AggregatePseudobulk(
  X,
  meta,
  replicate_col,
  group_col,
  min_cells = 10,
  assay_name = "counts"
)
}
\arguments{
\item{X}{matrix or Matrix
Gene-by-cell count matrix. Can be a base R matrix or a sparse Matrix
(from the Matrix package). Columns must be cell identifiers that match
the row names of \code{meta}.}

\item{meta}{data.frame
Per-cell metadata. Row names must correspond to column names of \code{X}.
Must contain the columns specified by \code{replicate_col} and \code{group_col}.}

\item{replicate_col}{character(1)
Name of the column in \code{meta} indicating the biological replicate (for
example sample or individual). Used as the first component of the
interaction that defines pseudobulks.}

\item{group_col}{character(1)
Name of the column in \code{meta} indicating the grouping factor (for example
cell type, cluster, condition). Used as the second component of the
interaction that defines pseudobulks.}

\item{min_cells}{integer(1), optional
Minimum number of cells required for a pseudobulk to be retained. Pseudobulks
with strictly greater than \code{min_cells} contributing cells are kept. Default
value is 10.}

\item{assay_name}{character(1), optional
Name to assign to the assay in the returned SummarizedExperiment. Default
is "counts".}
}
\value{
SummarizedExperiment
An object with:
\itemize{
\item assays: a named list with a single matrix-like assay (genes × pseudobulks)
containing aggregated counts. The assay name equals \code{assay_name}.
\item colData: a data.frame with one row per pseudobulk (metadata built by
\code{make_pseudobulk_metadata(meta, pb_groups)} and subset to kept pseudobulks).
Additional columns added to colData:
\itemize{
\item nCells: number of cells that contributed to each pseudobulk
\item nUMI: sum of counts across genes for each pseudobulk
\item nFeatures: number of genes with non-zero counts in the pseudobulk
}
}
}
\description{
This function aggregates a gene-by-cell count matrix into gene-by-pseudobulk
counts. Pseudobulk groups are defined by the interaction of a replicate
identifier and a group identifier (for example: sample_id × cell_type). The
function builds a sparse design matrix that maps cells to pseudobulks,
multiplies the counts matrix by that mapping to obtain aggregated counts,
filters pseudobulks with too few contributing cells, removes genes with zero
variance across the kept pseudobulks, and returns a SummarizedExperiment
containing assay(s) and per-pseudobulk metadata (including nCells, nUMI and
nFeatures).
}
\details{
Create pseudobulk samples by aggregating single-cell (or single-nucleus) counts
according to replicate and group annotations, and return a SummarizedExperiment.
\itemize{
\item Input checks:
\itemize{
\item \code{X} must be matrix-like (dense matrix or Matrix sparse object).
\item \code{meta} must be a data.frame with rownames matching \code{colnames(X)}.
\item \code{replicate_col} and \code{group_col} must exist in \code{meta} and contain no NAs.
}
\item Grouping:
\itemize{
\item Pseudobulk groups are created by interaction(meta[\link{replicate_col}],
meta[\link{group_col}], drop = TRUE). This produces factor levels representing
unique replicate × group combinations.
\item A sparse model matrix (~0 + pb_groups) is constructed to map cells to
pseudobulks. Columns of the resulting aggregated matrix are renamed by
removing the "pb_groups" prefix that is created by the model matrix.
}
\item Filtering:
\itemize{
\item The function computes the number of cells per pseudobulk (\code{n_cells}) and
keeps only pseudobulks with n_cells > min_cells (strict inequality).
\item Genes with zero standard deviation across the retained pseudobulks are
removed.
}
\item Post-processing:
\itemize{
\item The returned SummarizedExperiment's colData receives nCells, nUMI and
nFeatures. nUMI is computed as column sums of the aggregated counts.
nFeatures is computed after thresholding counts to binary (counts > 1
set to 1) and summing per column.
}
}
}
\section{Edge cases and warnings}{

\itemize{
\item If column names of \code{X} are not present in rownames(meta), the function
will stop and report the mismatch (it reports the missing cell ids).
\item If all pseudobulks are filtered out by the \code{min_cells} threshold, the
function will produce an empty SummarizedExperiment or fail in downstream
steps; callers should check the returned object.
\item The function assumes the existence of a helper \code{make_pseudobulk_metadata()}
in the calling environment or package; this function must accept the same
\code{meta} and \code{pb_groups} and return rownames corresponding to pseudobulk
column order.
}
}

\seealso{
make_pseudobulk_metadata, SummarizedExperiment, Matrix::sparse.model.matrix
}

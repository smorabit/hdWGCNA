# ScaleMetacells

Wrapper function to run Seurat's ScaleData function on the metacell
object.

## Usage

``` r
ScaleMetacells(seurat_obj, wgcna_name = NULL, ...)
```

## Arguments

- seurat_obj:

  A Seurat object

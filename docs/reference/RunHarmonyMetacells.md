# RunHarmonyMetacells

Wrapper function to run harmony's RunHarmony function on the metacell
object.

## Usage

``` r
RunHarmonyMetacells(seurat_obj, ...)
```

## Arguments

- seurat_obj:

  A Seurat object
